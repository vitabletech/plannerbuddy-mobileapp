import { StyleSheet } from 'react-native';

const getStyles = () => {
  return StyleSheet.create({
    header: {
      height: 40,
      marginTop: -20,
    },
    searchBar: {
      marginTop: 5,
    },
  });
};
export default getStyles;
